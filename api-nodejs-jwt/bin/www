#!/usr/bin/env node

const app = require('../app.js');
const config = require('../config/config');

// listen
app.listen(config.server.port, '192.168.33.50' ,() => {
    console.log('Server Running');
});
